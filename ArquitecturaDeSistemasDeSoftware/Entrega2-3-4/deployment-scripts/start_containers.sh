#!/usr/bin/env bash

set -e

# docker-compose build

# docker-compose up -d

# NOTE: if the above code doesnt work try this:
# /opt/bin/docker-compose -f docker-compose-aws.yml up -d > /dev/null 2> /dev/null < /dev/null &
